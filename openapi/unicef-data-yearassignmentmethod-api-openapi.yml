openapi: 3.2.0
info:
  title: RDM Year Assignment Method API
  description: Reference Data Manager API
  termsOfService: None
  contact:
    name: UNICEF
    url: https://data.unicef.org/
    email: data@unicef.org
  version: v2.0
servers:
- url: https://rdmapi.unicef.org
tags:
- name: YearAssignmentMethod
paths:
  /api/YearAssignmentMethods:
    get:
      tags:
      - YearAssignmentMethod
      summary: Get the list of all indicator year assignement methods
      operationId: YearAssignmentMethod_GetYearAssignmentMethods
      responses:
        '200':
          description: The list of all indicator year assignement methods, e.g. "Start of Fieldwork" or "End of Fieldwork"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/YearAssignmentMethodItem'
components:
  schemas:
    YearAssignmentMethodItem:
      type: object
      required:
      - yamId
      properties:
        yamId:
          type: integer
          format: int32
        name:
          type: string
        editedBy:
          type: string
        editedDate:
          type: string
        code:
          type: string
x-generator: NSwag v14.0.2.0 (NJsonSchema v11.0.0.0 (Newtonsoft.Json v13.0.0.0))
