openapi: 3.2.0
info:
  title: RDM Collection Process API
  description: Reference Data Manager API
  termsOfService: None
  contact:
    name: UNICEF
    url: https://data.unicef.org/
    email: data@unicef.org
  version: v2.0
servers:
- url: https://rdmapi.unicef.org
tags:
- name: CollectionProcess
paths:
  /api/collectionprocesses:
    get:
      tags:
      - CollectionProcess
      summary: Get the list of all collection processes/mechanisms related to indicators
      operationId: CollectionProcess_GetCMs
      responses:
        '200':
          description: The list of all collection processes/mechanisms related to indicators. Examples are "CRAVE" and "SDG Consultation"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CollectionProcessItem'
components:
  schemas:
    CollectionProcessItem:
      type: object
      required:
      - collectionProcessId
      properties:
        collectionProcessId:
          type: integer
          format: int32
        name:
          type: string
        editedBy:
          type: string
        editedDate:
          type: string
        description:
          type: string
        code:
          type: string
        dataRelease:
          type: string
        dataCollection:
          type: string
x-generator: NSwag v14.0.2.0 (NJsonSchema v11.0.0.0 (Newtonsoft.Json v13.0.0.0))
